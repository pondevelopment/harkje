/* Dev build info placeholder. */
export const BUILD_VERSION: string | null = 'dev';
export const BUILD_SHA: string | null = 'dev';
export const BUILD_TIME: string | null = new Date().toISOString();
